export type Post = {
	title: string;
	slug: string;
	description: string;
	date: string;
	published: boolean;
};

export type Guide = {
	title: string;
	slug: string;
	description: string;
	language: string;
	published: boolean;
};
